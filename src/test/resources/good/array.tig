/* A program to solve the 8-queens problem */

let
    var N := 66

    function printboard () = (print(chr(N)); print("\n"))

    function try (c : int) = (
     if c=4 then printboard () else (
     print(chr(N+c)); print("\n");
     try(c+1)
     ))
 in try (1);0
end
